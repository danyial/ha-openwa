# Copy to .env and fill in. .env is git-ignored.
OPENWA_URL=http://192.168.1.10:2785
# OPERATOR key, restricted via allowedSessions. Never commit.
OPENWA_API_KEY=
# Session id (UUID from GET /api/sessions), not the name
OPENWA_SESSION_ID=
